- [x] Add debug logging in pomodoro-timer.tsx for addFocusSession payload + response
- [x] Improve toast to show xp_earned/new_total_xp when present
- [ ] Re-test: complete a focus session and confirm XP + dashboard/sidebar progress update
- [ ] If XP still doesn’t update, trace backend DB writes (xp_transactions) and ensure getProfile uses the same user_id


